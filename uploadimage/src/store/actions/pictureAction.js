import React from 'react';

const pictureAction = (props) => {
    const {pictureURL} = props;
    return (
        <img class="materialboxed" width="650"  src='../../../dist/Img/bg.jpg'/>
    );
}

export default pictureAction;